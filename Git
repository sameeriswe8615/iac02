Git is version control system
Mulitple people contributing on code
code sharing withmultiple people
VCS maintain version history 
multiple version control system available in market
Git is distrubation version control system. ex. one code save in your local system it mean we can work offline 

#### Basic git commond #####
Git add : use to upload file in staging area
git commit : commit your code to locat version database 
git push : push code on git hub 

###############Practicle ##################
git config --global user.name "sameeriswe8615"
git config --global user.email sameeriswe@hotmail.com

git add test.txt
git status
git commit -m "this is test file" test.txt
git push -u origin master 
git config --list ( who done last changes ) 
git help config 
git config --help ( we can change verb over here ) 
git init ( use to check the repository where we are working ) 

#### Git Structure ###
 1) Working Directory (Stage fixes) 
 2) Staging area (Commit)
 3) .git directory ( Repository) 
 
 
 #################ADD file in staging area ####
 
 git add -A ( this commond use to add all file in repository ) 
 git status

##### Remove file from staging area #####
git reset "File Name"
get reset "this file will remove from staging area"

### Clonning a remote repo ####
git clone "Url"

### Changes in existing file ####

git diff ( this commond shows you existing file changes ) 

##### create Branch for desire feature ##########

git branch ( use to list all branch ) 
git brnach sam-repo ( to create new brnach ex. sam-repo )
git checkout sam-repo ( this commond use to check in new branch ) 
git checkout master ( to sing off from sam-repo and login to master branch use this commond )
git push -u origin sam-repo (This is commond use to push update from one file to another branch)
git pull origin master ( to retern the changes in master branch ) 
git branch --mearged (This commond use to mearge changes from sam-repo branch to master branch )

